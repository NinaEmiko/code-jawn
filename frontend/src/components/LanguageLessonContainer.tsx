const LanguageButtonContainer = ({props}:{props:any}) => {

    return (
        <div className="language-btn-container">
            <div className="lesson-btn">{props.lesson}</div>
            
        </div>
    )
}
export default LanguageButtonContainer;